# Array Merge
This project merges two integer arrays into a single combined array using a dedicated merge function.It also validates the size of each array before processing.

## What I Learned 
- How to merge two arrays into a third array
- How to pass multiple arrays to a function
- How to validate array size input
- How to use a counter to track position in the merged array
